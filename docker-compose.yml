name: ft_transcendence

include:
  - ./backend/compose-backend.yml
  - ./elk/compose-elk.yml
  - ./monitoring/compose-monitoring.yml

volumes:
  certs:
    driver: local
  esdata01:
    driver: local
  kibanadata:
    driver: local
  logstashdata:
    driver: local
  logstash-keystore:
  filebeatdata:
    driver: local
  app_logs:
    driver: local
  prometheus-data:
  grafana-data:

networks:
  backend:
    driver: bridge
  elk:
    driver: bridge
  monitoring:
    driver: bridge
