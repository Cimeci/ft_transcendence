export function LoginPage(): HTMLElement {
	const mainContainer = document.createElement("div");
	mainContainer.className = "important-text flex items-center justify-center h-screen"
	mainContainer.textContent = "Login Page";

	return (mainContainer);
}